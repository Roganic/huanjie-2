# 交互性能与界面重构记录

目标：完整处理常规操作等待模型、规则说明堆积和界面细节退化。范围为 GM 路由、命令元数据、前端展示/控制/辅助面板；单会话实施，未启用子 agent，保留工作区既有未提交内容。

结论与协议见 `../interaction-performance.md`。采用本地 ui-ux-pro-max 的暗色可读性、焦点和响应式建议，沿用 React，不引入 UI 框架或游戏引擎。

主要实现：
- `gm/host.py` 区分明确控制、交谈和自由输入；`game/commands.py` 保留统一事务，补充 0 调用反馈与历史来源。
- `ActionMessage.tsx` 统一正文/检定/规则详情；`SideSheet.tsx`、`useDialogFocus.ts` 处理面板与键盘，`PlayLayout.css` 为游戏主画面布局。
- `gameApi.ts` 直接接收 JSON，`App.tsx` 删除探索/战斗流解析与重复常驻面板；当前约 2981 行，后续需要拆组件时按职责继续，不做批量机械拆分。
- `EventTimeline` 完整进度进记录，主画面只呈现邻近实际后果/紧急事件；纯重试时间不再常驻。

验证：
- 最终全量报告 `/tmp/huanjie-ui-full-tests-final.txt`：716 passed，7 条既有依赖警告。
- 构建/lint/4 项 gameApi 测试通过，`git diff --check` 通过。
- `/tmp/huanjie-interaction-acceptance.json` 完整真实请求报告，`/tmp/huanjie-interaction-live.log` 汇总；16 操作/4 次模型调用、每步重放与存读档一致。
- 初次真实验收用问路句误要求必须进入交谈，实际只读分流正确；改用明确追问验证两阶段对话，不为验收修改路由。
- 隔离网页 5174 + 后端 8001 验证创建、真实交谈等待中查背包、阅读收益、装备、移动、三敌人防御、撤退、返回、存档/读档。响应式 375/768/1024/1440，完成焦点循环、Esc 返回入口。开发中变量遗漏导致的报错已修复；最终后续操作无新增错误。

运行与保护：
- 生产后端 PID 43011，HOST=127.0.0.1，PORT=8000；原前端 PID 11391，5173。
- `/tmp/huanjie-interaction-deployment.json` 保存当前 PID、备份路径和比对结果。备份 872 个原 JSON，启动与实际页面只读刷新后，仅两份会话的 `updated_at` 变化；未重置角色/任务/历史。
- 实际用户页面的场景读取恰逢重启失败，已通过只读“重试”恢复指引，未执行游戏操作。
- 验收临时服务和页面关闭；生产 5173 继续使用。密钥未写入任何报告或前端。

边界：自由文本仍需模型、此次实际追问 4.6 秒；没有实现 provider token streaming。关系/重试规则不属于本轮语义调整，模组编辑器与文本解析器后置。
