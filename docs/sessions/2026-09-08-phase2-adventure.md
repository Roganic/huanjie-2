# 2026-09-08 第二阶段完整冒险闭环

## 目标与范围

用户授权把第二阶段完整做好。保持现有基础系统、按钮快速通道、友方保护和用户存档，完成目标级行为裁定、人物事实记忆、可组合后果和多路线冒险。编辑器、纯文本解析器继续后置。

## 实施结论

- 扩展已有挑战契约而非增加并行规则体系；目标分开记录，做法和后果通过 ID 选择，冻结后再掷骰。
- 表达与目的性行动分离。表达没有物品、任务或好感副作用；普通交谈接口不再让特殊关键词拦截已明确分类的问话。离线旧文本适配器保留保守限制。
- 人物事实记忆单独保存，模型生成的对白保留为非权威对话素材。
- 新增内置余烬钟声，作为独立新冒险；新旧内容不会自动混用。
- 网页复用现有挑战区，用折叠选项选择做法/目标结果；通用社交收在次级折叠区，避免淹没故事目标。

## 真实模型发现与修复

实际调用当前配置的 Qwen 模型，先后发现：嵌套选项被输出为 JSON 字符串；可选 ID 被输出为空字符串；长历史导致复述旧主题；具体调查被选成普通交谈。

对应改为平铺模型工具参数、统一空选项默认语义、独立规划/叙事提示与较短对白窗口、明确行动意图类型及最多一次无副作用的规划纠正。没有增加针对示例玩家原话的匹配关键词。

## 验证记录

后端全量 738 项通过（7 项既有弃用警告），前端 build/lint/4 项请求恢复检查通过。维护脚本：`app/backend/scripts/accept_phase2.py`，输出 `/tmp/huanjie-phase2-live.json`。后端规则验收：`tests/test_phase2_adventure.py`。

网页隔离端口 8001/5174：已完成模组选择、新角色保留、支援做法/后果选择、按钮结算、失败后其他目标仍可用、场景切换、375px 无横向溢出与输入区可见；未发现控制台错误。

## 限制与下一步

语义仍有模型误判可能，格式校验不会证明任意对白正确。规则数值、事件写入、重放和存档由程序兜底。下一阶段按模组编辑 → 纯文本草稿解析推进，不继续堆散落的关键词特殊处理。

### 实际服务与收尾

- `accept_phase2.py --allow-fallback`：26 步/20 次模型请求，三结局与中途存读档一致，每一步幂等回放；直接操作中位 35 ms、最大 53 ms。该时间是本地规则与保存，不含浏览器渲染。
- 8 轮因上游资格错误降级，3 次明确挑战按钮恢复；其余降级已完成规则，只缺生成对白。成功返回计量为 42,546 输入/1,043 输出 tokens。没有把此结果当作零降级的纯 AI 通过。
- 单独捕获上游错误码 `403 AccessDenied.Unpurchased`，内容为无模型使用资格；原快照模型、同系列别名有成功也有拒绝，新版候选也被拒绝。未改动生产模型配置。
- 官方错误文档将该码指向服务开通/资格问题：[百炼错误码](https://help.aliyun.com/zh/model-studio/error-code)。现有证据不能证明具体账户为何间歇拒绝，需要核对控制台服务状态；不能直接断言需要再次购买。
- 本地 8000/5173 已更新，后端仅绑定 127.0.0.1。872 个原有会话/存档备份于 `/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-before-phase2-_2bz4kbm`；模型默认值归一化比对无游戏状态差异。部署记录 `/tmp/huanjie-phase2-deployment.json`。
- 剩余工作明确为：服务恢复资格后运行不带 `--allow-fallback` 的严格验收，继续评估当前廉价模型的理解稳定性。

- 收尾补齐 `read_only_subjects`（背包、角色、已接任务），避免文字查询没有合法工具入口；三项规则检查通过。真实读背包复验被上游资格错误拒绝，未改变状态，待服务恢复后补验。

- 最终补验升级前与过渡版本的请求摘要兼容：没有选择新选项的旧请求仍可回放；不同操作仍拒绝共用请求编号。最后全量 738 项通过。临时 8001/5174 服务与验收标签页已关闭。
