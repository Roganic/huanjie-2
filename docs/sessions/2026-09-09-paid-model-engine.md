# 充值后的 AI 验证、机制契约与地图引擎

本轮恢复完整目标。此前规划与官方套餐调查提供了新的实施依据；本轮属于实际进展，不再沿用免费额度/锁屏阻塞。完整目标保持未完成，尤其不能把地图迁入 Phaser 视为整个游戏前端完成。

## 已完成的改动

- 原百炼配置从已知安全备份恢复，模型改为 `qwen3.7-plus`，仅保存于忽略的后端配置文件，权限 0600。旧 GLM 配置安全备份位置为临时目录 `huanjie-glm-x6spy7aw.env`，不打印或复制到发布文件。
- 原生模型适配器统一接入持久 SQLite 费用账本；网站模型网关接入 D1 全局账本。原子预留覆盖并发、重试、断线和无 usage；解析/格式不合格仍计费，成功 usage 才能结算释放差额。只有核验过的模型、供应商和地域允许付费。
- 本地硬上限 20 元，网站硬上限 25 元，5 元保留对账；初次筛选本地阈值 2 元、网站默认 0 元。隔离云端模拟器也强制 0 元，防止新建 QA 数据库重置付费额度。
- 叙述契约纠正“任务信息已经在界面展示”的错误假设：默认正文需要回答玩家的问题，不能把关键线索仅留在折叠规则里。允许说明已知选择，同时不能替玩家决定下一步。
- 故事图中自动事件与鉴定采用可区分 Schema；`requires` 为 AND，`requires_any` 为 OR。编译器把 OR 汇合转换为各前置结果产生的共享标记，沿用现有规则。成功/失败冲突及无前置任务继续拒绝。
- 编译器不再把 story/region 事件绑定到原场景；互动时间按作者的 `time_cost` 结算；立即生效的结果不能反向携带延迟取消条件。整个故事生成单次 90 秒、含修复总计 180 秒，NPC 交互等待限制未放宽。
- 地图迁入 Phaser 3.90，确定性不规则路网布局、节点命中、相机平移/缩放、当前位置标记与移动请求接通；保留浮窗操作和可访问的地点列表。旧方格、SVG 连线和 CSS 箭头代码移除；背包仍依赖的通用面板样式改由背包自身导入。
- 工坊支持上传 TXT/Markdown 原稿，原始 JSON 上传编辑保留；文档 Schema 已同步互动耗时字段。
- 账号调研已记录在 `docs/account-options.md`：首个 Sites 版本可复用平台身份；独立注册可选择 Supabase/Clerk 免费档，尚未创建外部应用，也尚未通过线上实际登录验收。

## 真实模型证据

- `check_gm.py` 工具握手通过：输入 329 / 输出 30 tokens。
- `accept_short.py --ai` 两次通过 22 步三路线：亮灯、失败复原与读档、钟声。第二次报告 `/tmp/huanjie-short-ai.json`，日志 `/tmp/huanjie-short-paid-final.log`。NPC 约 1.6–2.1 秒，目标行动规划+叙述约 5 秒。使用固定骰覆盖特定成败，不是随机成功率或长期速度承诺。
- 第二次人工复核：岑婆明确回答修灯棚/潮阶/敲钟路径，阿禾正确回应成功交涉，奖励与任务状态一致；不再出现旧免费模型的明显意图误判和角色倒置。仍需更广的自由输入和连续追问验收。
- 整篇解析初次 45 秒超时；90 秒单次上限后返回结构，但存在条件错误。加入 OR 汇合后，`/tmp/huanjie-paid-parser-joins.json` 曾返回结构合法稿；人工复核发现倒计时取消条件反向，不算语义成功。
- 最新 `/tmp/huanjie-paid-parser-cancellation.json` 仍未通过：模型在两次生成/修复后遗漏任务实际前置。没有导入错误模组，也没有用手工模组替代生成结果。下一步应继续完善结构生成与机制审阅流程，不降低校验要求。
- 本轮账本最终读取：24 次请求；按已报告 usage 计约 **0.326256 元**，一次超时保留 **0.189824 元**，保守占用约 **0.51608 元**。这不是已对账的供应商实际账单。

## 验证

- 后端全量 770 项通过，7 个历史警告；日志 `/tmp/huanjie-full-budget-joins.log`。
- 全量之后新增即时取消保护、预算读取和供应商/地域价格限定：作者/费用相关 29 项及模型适配相关 37 项分别通过，不推算为新的全量数字。
- 网站 6 项检查通过：玩家存档隔离、并发提交、损坏内容、全局模型预算、未知请求成本保留与请求体不能覆盖计费/模型。
- 最终地图浏览器报告：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-atlas-engine-wkxYBs/report.json`。实际 Canvas 点击相邻节点并完成移动；平移、缩放、重新打开、390px 手机无页面溢出；截图已人工查看。
- Markdown 原稿上传单独通过真实浏览器检查，没有为上传测试触发模型请求。
- 前端 lint 与 Sites 构建通过；Sites 未保存版本或部署。加入新预算模块后，发布脚本同步复制 `cloud/budget.mjs` 及新 D1 迁移。
- 本地原 8000 服务已更新，5173 前端热更新。872 份会话/存档备份并归一化比较无事实变化；最近备份清单和最终 PID 见 `/tmp/huanjie-phase2-deployment.json`。本地 AI 已启用，阈值 2 元。
- 8002 隔离模拟器保留原 QA 数据目录，进程会话 13014；5175 QA 前端继续运行。模拟器是在最后计价地域校验之前启动，下次使用云端模型逻辑前应更新该已知进程（当前其付费上限始终为 0）。

## 仍需完成

1. 游戏主舞台整体引擎重构。目前主界面仍是 React，只有地图迁入 Phaser；不能以换皮或局部 Canvas 宣称完成。
2. 完整原稿可靠生成、机制忠实性审核、导入及实际试玩。当前最新生成仍不合格，短中篇手工内容继续保留。
3. 更广连续自由行动验收、网站实际账号登录与恢复、最终同源源码包、Sites 部署及线上 AI/保存验证。

预算保护、模型可用和图形引擎路径已具备，剩余是可继续推进的实现/质量工作，无需重新等待免费平台授权。

最终服务 PID 90196，模型配置有效，872 个原文件比对通过。最后 Sites 构建成功，最新发布源码快照为 `/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-site-source-rWZH1z`；该快照已包含 Phaser、预算模块及新迁移，但尚未在独立目录重新安装构建或上传部署。所有付费测试进程已经结束。
