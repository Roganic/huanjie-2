# 文本解析与原稿机制验收

本轮识别的失败并非 JSON 语法：生成结果会把检定写成自动互动、将操作耗时写成延迟后果、用重新进入地点代替成功事件、重复发放任务奖励。两次非思考的生成/审查均通过结构检查但未通过原稿语义复核，没有安装到用户冒险。

已实现生成→受限 JSON Pointer 修订→编译校验，并保留原文摘录与机制对应、假设、作者问题。工具只能修订草稿，不能写游戏状态或执行脚本。结局增加 forbidden_flags，编译器检查与前置路径的冲突；编辑器提供中文字段和生成时审查记录。

网站模型代理此前仅识别 submit_module，导致 revise_module 只能得到普通对白的 800 token / 18 秒限制。已统一识别两个创作步骤，输出上限仍受服务端控制。

当前验证 Qwen3.7 Plus 有界思考模式，仅用于创作生成/审查，普通行动和对白保持非思考模式。思考预算最多 4096 tokens，并纳入请求前费用预留。按官方北京地区价格，思考与最终输出单价相同，保守使用输入2/输出8元每百万，不应用活动折扣。

新增 scripts/accept_parsed_short.py：对实际模型输出文件做哈希留证，导入独立运行目录，检验社交、调查成功、失败复原、敲钟、延误亮灯、延误敲钟六条路线，检定DC、时间成本、首次交谈安排倒计时、取消、存读档、幂等、奖励一次均验证。脚本不修改生成结果，不调用模型；已确认会拒绝缺少调查检定的生成稿。

未完成：有界思考真实调用尚待结果、实际生成模组全路线验收、创作网页复核、Sites 发布/身份/保存验证。前端构建及lint、48项相关后端检查和7项云端检查通过；有界思考预算变更后的检查另行跟进。

官方依据：https://help.aliyun.com/zh/model-studio/model-pricing 与 https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions 。

## 后续修复与实证

- 同次交谈的立即事实现在能触发同次交谈的依赖事件，与 JSON 排列顺序无关；不会凭空触发进入地点或提前推进倒计时。延迟互动不再提前显示最终后果，立即互动不重复显示同一段叙述。
- 全量后端 779 项通过（`/tmp/huanjie-parser-full-final.log`），后续适配/费用与流式相关 23 项通过，网站云端 8 项通过。流水号不是验收范围的替代，功能验证见脚本。
- 创作网页验证通过：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-studio-mapping-bKbNjL/report.json`。真实生成过的审查对应可展示、编辑、校验、保存、跨刷新恢复并导出；这是工坊流程证据，未将这份有机制错误的生成稿当作合格冒险。
- 本地服务已更新事件/编译初版，892 份既有会话/存档事实核对一致，备份位于 `huanjie-before-phase2-4nql2l5p` 临时目录。随后新增的流式适配仍需完成真实验证后更新本地服务。
- 有界思考非流式调用出现一次 90 秒超时、一次约 180 秒格式失败（账本已记录 6683 输入 / 10518 输出），以及一次 180 秒超时。未返回 usage 的费用保留预留，未将失败视为免费。
- 改为仅创作步骤启用流式工具参数接收，在服务器聚合完整文档后校验；限制 4096 思考 tokens，单阶段最长 300 秒、两阶段总时限 600 秒。普通游戏请求仍为短时非思考调用。中断、截断、缺少结束标志的流不能安装为模组。思考内容不进入游戏消息。
- Provider 对单一创作工具的纯 JSON 文档兼容，仍严格校验字段并走同一个编译器；游戏动作没有这条兼容路径。返回格式错误但带 usage 时，解析结果也记录实际用量。
- Sites 当前仍为仅所有者可见、版本 0、未部署；独立源码预构建成功，但在加入流式模块后须重新生成发布快照，旧 `huanjie-site-source-KLxx6b` 不可直接发布。

## 最新结果核对

- 草稿修订改为稳定条目标识定位，避免删除前项后数字下标错位；整批失败不改变草稿。人物知识解锁引用实际剧情事件，交由编译器生成条件。最新相关检查 30 项通过（`/tmp/huanjie-stable-repair-tests.log`），不能将先前 779 项全量结果声称为这些新增变更后的全量结果。
- 继续修订保留草稿的真实调用已终止：约 210.2 秒，`model_unavailable`，连接或工具格式失败；输出 `/tmp/huanjie-parser-stable-repaired.json`，没有报告 usage，因此不能视为零费用。尚未获得通过六路线验收的生成稿，不重复启动同一请求。
- 工坊浏览器验收完成：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-studio-mapping-XdVvwL/report.json`。包括原文对应、编辑保存和刷新恢复，以及零额度下智能修复失败时保留原稿与未完成结构；没有浏览器错误。这证明草稿恢复流程，不代表模型生成质量合格。
- 用户要求对齐大阶段，继续按可靠 AI 冒险、游戏体验、模组创作、线上发布划分验收。API 套餐调查与开发费用分开，不因折扣宣传另行购买。
