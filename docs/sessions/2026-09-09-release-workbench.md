# 完整版本推进：创作工坊、网页引擎与交付验证

目标仍是 `docs/release-plan.md` 的全部交付，不能把局部路线或模型问候通过算作完成。本轮仍由当前 session 串行维护前后端及 Sites，未启动其他 agent。

## 已落地

- 创作：纯文本解析端点、共享 Schema 表单、校验/修复、草稿保存与恢复、导出/导入、复制已有模组。模型补全假设和待确认问题随草稿保留；修改后返回会保存。相同内容重复导入幂等，不同内容不能覆盖同一标识。
- 内容：`最后一盏渡灯`（短篇）和 `潮汐之下的城`（中篇），原稿位于 docs/modules，可玩数据在 backend/src/content/adventures。非战斗任务依赖实际标记，避免所有目标被强制解释成杀敌。
- 游戏呈现：创建角色聚焦姓名与职业；属性和能力折叠；场景直接呈现可用挑战；多行输入；主画面展示终章，支持回顾和继续探索。
- 网站运行：Pyodide 在独立 Worker 中运行原 Python/FastAPI 规则，不重写为另一份裁定实现。模型密钥只在网站服务端。D1 记录用户快照版本，R2 保存内容；身份来自 Sites 调度层。
- 持久性：云端保留当前冒险选择，不依赖新浏览器的本地选择；云端会话不按临时会话超时删除，纯读取不写入进度。并发重试使用独立对象键，避免失败请求删除成功请求的数据；冲突拒绝覆盖。
- 发布源码准备脚本只复制前端、规则代码和模组数据，不复制密钥、存档或本地缓存。已在全新临时目录安装与构建成功；尚未发布。

## 验证证据

- `scripts/accept-browser.mjs` 在隔离云端模拟环境通过：短篇钟声路线、刷新保留结果、新浏览器上下文恢复、桌面/390px 手机、模组复制/编辑/返回保存/重开/导出，原冒险保留，无页面异常。
- 报告：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-browser-acceptance-g2E6GG/report.json`，旁边有截图及导出模组。
- 该次两次 NPC 调用分别 2.24/2.34 秒，均返回模型结果。**人工复核发现交付后的 NPC 台词出现玩家视角，故不算 AI 质量验收通过。** 已明确提示词中的发言人/听者边界，待复验。
- 4 项云端保存测试通过：身份隔离、重试/并发、缺失对象、路径与大小验证。
- 全量第一次为 750 通过/3 失败，失败均为旧“相同内容重复导入必须 409”断言。已按新幂等契约更新并保留不可覆盖检查，相关 101 项通过；没有删除边界保护测试。移除与已有导入测试重复的新测试。
- 前端本地和 Sites 构建已通过；当前最后一次改动后需在正式发布前重新生成同一来源的构建和源码快照。

## 模型结论与未完成

- 已确认的 BigModel 密钥仍只保存在忽略的本地配置，未知来源的备用密钥未发送给猜测的平台。
- GLM-4.7-Flash、4.6V-Flash 真实请求遇到 429/1305。旧 glm-4-flash、250414、4.5-flash 有基本工具调用成功，但复杂请求会忽略工具或超时；4.5 官方已注明下线后转到 4.7，不能当独立长期备份。
- legacy Flash 的严格 JSON 包装仅是传输兼容层，普通文字不能被当作已执行行动。生成整份模组改用结构化 JSON 参数输出，避免嵌套 JSON 字符串与工具选择歧义；完整 Schema 仍约束文档，parser 做实际校验，仍需真实成功解析后验证语义。
- `accept_short.py --ai` 的目标交涉路线仍未通过；模型对白视角和文本解析可靠性未达交付条件。
- 未发布 Sites；项目 ID 沿用 `appgprj_6aa0290ab8248191a0772a169e8bcc0a`，不能重复创建。

## 后续

1. 解决模型质量/容量并用同样场景复验；不能用降级或仅问候成功冒充合格。
2. 完成真实故事解析、导入并试玩；核对故事因果与结局，而不只看 Schema。
3. 继续创作界面各类别、窄屏和错误修复验收，处理剩余交互问题。
4. 完成同源发布构建、私有部署、线上模型/保存/玩法验证，逐项完成目标审计。

后续检查：全量 752 项通过（34 秒）；随后结构化生成适配的相关 12 项通过。新增了独立结构化生成边界检查，前端 lint 和 4 项云端保存检查通过。

## 本地更新

已更新原 8000 服务，5173 沿用原前端热更新。重启前备份 872 个文件到 `huanjie-before-phase2-y21dljk1`；逐文件比较通过，只有默认会话保存时间更新，角色/故事/存档事实无差异。

纯文本解析最新实际输出已有草稿，但不合格：中文标识、列表/映射混淆，且出现接任务即产生完成标记、经验误写为物品、无条件结局等语义问题。没有将其安装为可玩模组。下一步应把叙事目标/触发条件的中间表示交由确定性编译器生成标记、奖励和结局依赖，减少直接让小模型手写整套底层数据的负担；保留高级原始契约编辑和不能确定的作者问题，不静默删减故事。

最新严格 AI 复验仍失败：模型将“争取信任并索取校准图”的目标行动判作 expression，同时台词仍出现玩家视角。规则主机没有额外给出物品或跳过挑战，但这不是合格体验。需要继续改善意图与角色输出契约/模型选择，不能靠按钮路线通过掩盖。最新网站构建成功，构建产物未包含配置中的真实密钥。

保留服务：原后端 8000 已更新；原前端 5173 沿用；隔离测试前端 5175、云端模拟 8002 仍运行。网站源码临时快照 VMD2hK 已验证可独立安装构建，但后续新增改动后须重新生成用于正式发布的快照。


## 故事编译与创作恢复（本轮续做）

- 新增 `content/compiler.py`：StoryGraph 明确地点、人物、物品、剧情事件、任务和结局。编译器生成稳定标记和已有 ModulePack，实际裁定继续走原规则。
- 检定与自动事件分开；鉴定必须有风险与失败分支；按实际结果生成依赖。拒绝引用错误、循环、矛盾成功/失败依赖及不可达地图。效果改为按类型区分的结构，避免把经验、物品状态写进不兼容参数。
- 解析失败保留 outline、作者假设/问题。新增 `story_graph` 的确定性修复入口；草稿 `pending` 保存未应用的结构或 JSON，含不完整 JSON。工坊可以恢复、修改、应用后导出，未应用时禁止误导入旧模组。切换草稿/复制模组前先保存已有编辑。
- 分类编辑的浏览器检查揭示 textarea 可访问名称包含旧内容，已给字段明确标签；手机标题按钮不再挤成单字换行，JSON 文件保留原缩进并横向滚动。
- 浏览器报告：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-studio-kdNLlF/report.json`。地图、人物、物品、任务、事件、结局编辑/校验通过；无效文件跨刷新保留；修复后导出；390px 无页面溢出，无页面异常。
- 全量 762 项通过（34.13 秒）；最后一项主持传输变更之后，相关 26 项通过。不是 AI 语义验收。

### 真实模型证据

- GLM-4.7-Flash 简短工具请求仍超时。
- 将已有目标挑战直接放入 action 清单（排除通用 social）后，legacy Flash 有一次正确选择 reassure-he 并走完前两条短篇路线；第三条首轮对白超时。对白还出现玩家视角，不能算通过。
- 叙述阶段改为直接结构化 message，不再要求选择唯一 respond 工具，并在系统角色分配中明确当前 NPC。复测对白视角改善，但相同目标输入再次被错误归类为 expression，故意图稳定性仍未通过。
- 原信件故事的真实解析仍失败，存在错误效果、缺失地点、失败分支混淆和经验当效果等问题。编译器拒绝安装，没有通过删减故事或发放假奖励掩盖。
- Pollinations 官方现在要求 API Key，需登录领取；魔搭官方提供免费 API 入口，也需账户 Token。没有使用泄露密钥或把未知备用密钥发送到猜测的平台。
- CUA 实测 Mac 仍锁定，已异步请求用户手动解锁以继续正常登录授权；编码、隔离浏览器与测试不依赖解锁。

后续仍需：可靠模型接入/连续短篇语义验收、真实原稿解析后导入并试玩、剩余创作修复 UI 及中篇可选战斗验收、Sites 正式私有部署与线上验证。总目标保持进行中。


### 本轮最终证据

- 最新工坊浏览器报告：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-studio-2Ac3Ur/report.json`，四组检查全通过，包含初始补给编辑及失败 StoryGraph 的恢复、作者问题、修复、确定性编译。没有用伪造模型结果声称 AI 通过。
- 中篇可选双敌人遭遇补验通过：完整遭遇人数、先攻、真实装备伤害计算、胜利 120 经验与两件掉落、操作重放不重复结算、退出战斗后重返不刷新敌人。固定随机骰用于可重复验证，不更改角色或敌人生命。
- GLM 规划/解析温度固定 0.1、叙述 0.5，云端与本地一致。官方参数说明：https://docs.bigmodel.cn/api-reference/模型-api/对话补全 。降低默认随机性后仍误判目标行动，250414 仍产出 xp 非法效果与错误事件类型，故不能靠参数调整宣告质量合格。最后解析报告 `/tmp/huanjie-parser-low-temperature.json`，45.1 秒，两次调用，未导入。
- 最后主持/设置相关 33 项通过，云端保存 4 项通过，前端 lint 与最终 Sites 构建通过。全量 762 通过是在新增中篇遭遇检查之前；没有将加法推算冒充全量重跑结果。
- 原 8000 服务已更新至本轮代码，PID 64778；872 个原会话与存档备份到 `huanjie-before-phase2-waqoltwt`，比对无事实差异，仅默认会话保存时间变化。构建中的 13 个文本产物检查没有当前真实密钥。
- 5173 原前端继续热更新。5175 的隔离浏览器环境继续保留；8002 云端模拟进程仍载入本轮 temperature 调整之前的服务代码，下轮使用时应验证并重启该已知模拟服务（不要扫描或动用户服务）。Sites 尚未推送源码/保存版本/部署。
- 当前无仍在运行的真实模型测试；所有本轮有 session_id 的模型/测试/构建已结束。Mac 解锁问题尚未答复。此轮属于有进展，不能据此将总目标标为 blocked 或 complete。


## 发布准备收口与外部阻塞复核

上一目标回合有实际进展。本次继续复核：Mac 仍锁定，CUA 再次明确要求手动解锁，不能进入第三方平台正常登录。已连续多个目标回合遇到同一模型授权/可用性阻塞；已提供的 GLM 服务无法通过实际意图及文本故事验收，未知发行方备用密钥仍未使用。

已完成其余可以独立推进的发布准备：

- 更新已知 8002 隔离云端模拟服务，保留原临时存储；新进程会话为 17250，健康入口返回 disposable=true。原 8000/5173 未动。
- 从当前主工作区生成独立发布源码目录 `/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-site-source-y2K5eA`。178 个源文件逐字节一致，另有 2 个由打包脚本生成的说明文件；文件哈希清单 `/tmp/huanjie-release-source-manifest.json`。
- 在该独立目录重新安装依赖并构建成功，日志 `/tmp/huanjie-stage-final-install.log`、`/tmp/huanjie-stage-final-build.log`。这证明源码包可独立构建，不证明 AI 合格或网站已经发布。

当前剩余交付必须等有效模型服务：连续短篇 AI 语义验收、真实纯文本故事解析/导入/试玩、完成 Sites 部署及线上 AI 和持久保存验收。继续对同一低能力免费模型重复改措辞/放宽校验，或用手工模组冒充解析成功，都不符合目标。待用户解锁完成正规平台登录，或提供已授权且明确发行方的可用模型配置后恢复。总目标未完成，不能标为 complete。
