# Sites 实际发布与创作质量验收

## 已完成

- 已发布私有网站：https://huanjie-adventures.bigbiggod.chatgpt.site 。Site 所有者唯一可访问；没有扩大受众。保留同一 Site、数据库和对象存储。
- 发布快照独立于主仓库脏工作区，不含本地密钥、冒险或存档。首版源码 b9edaf711763c857fdbfd9e1dcd62c1ebffd30fd；第二版 ca6c7e045ba7c0f02b0a330afba868d146e42708；第三版 1bb601e3df4ad9fc869e8d9c19ffd96e4b469148，均成功推送、打包、保存并发布。
- 用户实际完成 ChatGPT 登录，游戏随后成功创建“渡灯旅人”、开始短篇、移动、无检定敲钟、报告并到达“听见归航”终章。匿名、伪造用户头与仅持有测试访问凭证的 API 请求均为 401；没有绕过用户身份检查。
- 云端 D1 存档索引实际存在，用户操作导致版本递增。已执行显式保存；刷新、真实模型与工坊的最终验收继续进行。

## 线上模型问题

最初模型请求失败，规则结果正常保留，因此不计为真实 AI 试玩通过。线上日志明确定位为 Cloudflare Workers 不支持 fetch 的 redirect=error。改为 manual，并将任何非 2xx（包括重定向）拒绝，密钥不会跟随到新域名；测试覆盖该行为。新增日志只记录错误类别与经脱敏的连接错误，不记录请求、对白或密钥。

第三版已包含此修复，并配置分开的模型：日常游戏 qwen3.7-plus；模组创作 qwen3.8-max。两者同服务同账户，服务端选择模型，客户端不能指定任意模型或放宽预算。本地累计阈值 6 元；网站累计阈值 5 元；均在此前本地20/网站25/对账余量5元的总50元范围内，不重置账本、不购买。

## 文本解析实证

更强模型的非思考修订仍出现机制错误，未视为通过。最后直接从《最后一盏渡灯》原稿重新生成，使用 qwen3.8-max 的 4096 token 有界思考、最多一次生成与两次修订，实际3次调用、约382.78秒，输入29711/输出17062 tokens，按官方北京12/36元每百万计费约0.970764元。

实际输出 `/tmp/huanjie-parser-max-fresh.json`，SHA256 `ce95ffa2444bef5bc0a4e3f149600a97f2b09202f5341e65604c6fd5ff3562a3`。未手工改写生成结果。六条规则路线已通过：社交、调查成功、调查失败复原、敲钟、延误亮灯、延误敲钟。检查两项DC10、失败后的实际耗时、首次交谈启动倒计时、引航取消倒计时、分支结局、80经验/一枚纪念章、请求重放及存读档。

证据：`/var/folders/32/7b2_5pds57701rzzwvtc6f6h0000gn/T/huanjie-parsed-short-geqp4sge/report.json`。原始生成结构、编译模组和审查对应保存于 `docs/modules/generated-last-ferry-light.*.json`。

验收脚本自身纠正两处无效假设：不按动作中文名称含“修/校/调”识别点灯，而按三条校准来源；不通过反复交谈推进倒计时，改为真实移动。原规则中普通交谈不消耗世界时间。未降低实际机制要求。

生成审查还保留两项作者说明：同时点灯/敲钟时取点灯优先；对于社交重试，模型的问题描述不准确，编译后的挑战仍统一 retry=once，不允许反复掷骰。原稿没有相反要求。这说明生成审查记录需要作者核对，不能视为规则权威。

## 验证范围

- 发布前最新完整后端786项通过；随后稳定set修订、修订错误反馈、独立创作模型相关28项通过。
- 网站边界9项通过，包括身份隔离、并发保存、费用预留、模型选择和禁止跟随重定向。构建与lint通过。
- 本地服务已更新，892份既有会话/存档事实比对一致。最新备份 `huanjie-before-phase2-l6sypa66`，PID99876。
- 本轮尚未将整个目标标记完成；最终真实线上AI、编辑导入试玩与恢复仍需验收。

## 线上收口补验

第三版的真实 AI 交谈与追问已成功，云端记录了模型返回的实际 token 用量。询问七人获救后的货物状况，岑婆保持货物进水的既有结局，没有再发奖励。

实际生成的 ModulePack 已在在线工坊通过文件上传、应用、格式校验与保存。试玩安装暴露 Pyodide 不提供 os.link；存储层在单 Worker 串行环境改为原子 rename，本地仍保留 hardlink 防覆盖。新增浏览器安装/重复导入/冲突拒绝检查，相关49项通过。界面也不再直接展示 Python 堆栈。修复版本构建/lint通过，正在发布与复验。


第四版已修复安装并完成实际上传草稿的独立冒险启动，新页面完整恢复旧冒险和云端草稿。生成模组中 NPC 却沿错误旧对白把渡钟说成在不存在的钟楼，因此第五版增加统一 known_places：当前/相邻/已知任务地点，以及已满足条件的设施名，规划与叙事共用。不会输出隐藏地点、未解锁设施、结果或秘密。相关27项通过；并发叙事测试改为检查所用场景快照，而非禁止提示词包含相邻地点名。第五版源码80d5ce71ef70197cfe5fdee9d5b784c46dffa763成功发布。原892份会话/存档仍无事实差异。


## 完整试玩与末次检查

生成稿已通过线上“交谈—追问—节点地图到灯塔—三短一长—回渡口报告”流程，实际模型回应成功、结局“听见归航”、时间3、角色经验80、背包一枚渡口纪念章。生成稿启动在新冒险，旧通关记录与草稿继续保留；刷新后角色/历史/地点恢复。地图关闭辅助面板后不再穿透误选。

第六版进一步把提及“系统提示/上下文”的内部解释纳入统一回应检查，最多修订一次而不执行额外行动；不保证生成语言零误差。第七版仅修正角色面板按职业显示能力，不再让战士显示偷袭。最终后端791项通过（39.51秒），云端9项、构建和lint通过；原892份存档事实一致，最新本地PID2115。版本7源码8293537f65911e4596260c867b34a5403b331af7。

线上又上传完整1105字原稿，使用真实创作模型生成，当前在受限审阅流程中。此前本地通过的真实生成稿与这次线上新生成稿分开记录，不能混同两份验收结论。


## 线上原稿解析结果

网站实际上传1105字Markdown原稿完成一次生成和两次审阅，约7分钟，3次返回均包含usage：输入29056、输出15888 tokens，按北京官方价格约0.920640元。结果显示格式校验通过、4地点、2人物、任务与3结局、20项原稿对应记录。已保存为“最后一盏渡灯 · 在线审阅稿”，新标识story-3b05aac346a62a61-online，未替换已安装或正在运行的冒险。

在线新稿简介把“七人，其中一个孩子”写成“七名乘客和一个孩子”，通过编辑器纠正简介，并调整名称/ID以区分草稿。规则未手改；此稿未重复执行六路线验收，不能借用上一份生成稿的通过结果。模型还补全了原稿未定义的连接、复原记录及关系数值，这些补全在审阅区保留供作者确认。纯文本解析是草稿创作辅助，不能承诺任意故事一次生成即可机械与语义全对。

本轮最新全量791项、网站边界9项、构建与lint通过；独立游戏引擎、短中篇内容、真实AI流程、模块编辑与文本解析、实际平台身份与云端恢复、Sites私有发布均有对应证据。真实跨账号多人验收未开展；当前网站只有所有者，平台身份隔离以服务端测试及未认证访问拒绝补充验证。


## 交付结论

最后在第七版刷新恢复后，真实 AI 对“渡钟到底在哪里？”回答：“渡钟就在灯塔里。你刚才正是去那里敲响了它，才引渡船平安靠岸。”没有内部解释，世界时间仍为3；角色面板仅显示战士能力，经验仍80。在线审阅稿刷新后可重新载入，名称与新ID一致。保留唯一游戏页面24；原页面22此前已关闭，因此新开24接回真实登录状态。

本轮完整范围已完成。API已报告3.576644元，保留未知预留0.984404元，保守合计4.561048元。线上创作仍为需审阅的草稿；当前网站只有所有者访问，未宣称完成真实多账号/多人安全验收，也不提供独立邮箱注册。API预算设置保持本地6、网站5，未自动提高或购买。
